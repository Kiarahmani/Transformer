CREATE TABLE A (
    id          SMALLINT NOT NULL,
    balance      SMALLINT  NOT NULL,
    PRIMARY KEY (id)
);

CREATE TABLE B (
    id          SMALLINT NOT NULL,
    name        VARCHAR(32),
    PRIMARY KEY (id)
);
